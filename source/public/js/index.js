/**
 * Created by JuSanchez on 27/01/2016.
 */

/*$(document).ready(function() {
    $.get('/trazabilidadMotorola/production/line/' + dateT + '/' + lineaIzquierdaId, function (linea) {
        $.each(linea, function (key, value) {
            totalIzquierda += parseInt(value.Total);
        })
        $('#producidoIzquierda')
            .text(totalIzquierda);
    });
    $.get('/trazabilidadMotorola/production/line/' + dateT + '/' + lineaDerechaId, function (linea) {
        $.each(linea, function (key, value) {
            totalDerecha += parseInt(value.Total);
        })
        $('#producidoDerecha')
            .text(totalDerecha);
    });
}*/